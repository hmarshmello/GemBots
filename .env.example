# Flask Configuration
FLASK_DEBUG=1  # Set to 0 in production
FLASK_SECRET_KEY=your_secret_key_here  # Generate a secure random key
CSRF_SECRET_KEY=your_csrf_key_here     # Generate a secure random key

# Gemini AI Configuration
GOOGLE_GENERATIVEAI_API_KEY=your_gemini_api_key_here

# Rate Limiting (optional, defaults in config.py)
# RATELIMIT_DEFAULT="200 per day;50 per hour;1 per second"

# Server Configuration
PORT=8000  # Default port for local development

# Security Settings (optional, defaults in config.py)
# SESSION_COOKIE_SECURE=True
# SESSION_COOKIE_HTTPONLY=True
# SESSION_COOKIE_SAMESITE=Lax
# PERMANENT_SESSION_LIFETIME=3600

# File Upload Settings (optional, defaults in config.py)
# MAX_CONTENT_LENGTH=104857600  # 100MB in bytes
# MAX_FILES_PER_FOLDER=1000

# Note: Generate secure keys using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
